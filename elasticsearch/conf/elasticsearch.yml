
cluster:
        name: "ES"

path:
        data: /var/lib/elasticsearch
        logs: /var/log/elasticsearch

node:
        name: "segu-elastic"

network:
        host: 0.0.0.0

discovery:
        type: "single-node"

bootstrap:
        memory_lock: True
